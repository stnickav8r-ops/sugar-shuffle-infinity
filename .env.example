# Sugar Shuffle Infinity — copy this file to ".env" before npm run dev
#
# The game does not use accounts or a database. Keep auth off and
# leave DATABASE_URL unset so a local clone matches the shipped build.

VITE_AUTH_ENABLED=false
